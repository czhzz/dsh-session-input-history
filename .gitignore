# dependencies
node_modules/

# Build output. `lib/` is intentionally NOT ignored: the compiled host half and
# the bundled client are committed so `dsh plugin add github:…` needs no local
# build and no pnpm build approval.

# TypeScript incremental build cache
*.tsbuildinfo

# local caches / logs
.npm-cache/
*.tgz
npm-debug.log*

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/
