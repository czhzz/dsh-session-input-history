# dependencies
node_modules/

# build output (regenerated by `prepare` on git install, `prepack` on npm publish)
lib/

# TypeScript incremental build cache
*.tsbuildinfo

# local caches / logs
.npm-cache/
*.tgz
npm-debug.log*

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/
